import { IVideo } from "@designcombo/types";

/**
 * User Video Library
 * Videos are now loaded from user uploads via the upload system
 * Demo videos have been removed - users add their own content
 */
export const VIDEOS = [] as Partial<IVideo>[];
